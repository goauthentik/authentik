version: 1
metadata:
  name: Default - Request flow
entries:
- attrs:
    designation: stage_configuration
    name: Request accecss
    title: Request accecss
    authentication: require_authenticated
  identifiers:
    slug: default-request
  model: authentik_flows.flow
