version: 1
metadata:
  name: Default - User selection flow
entries:
- attrs:
    designation: user_selection
    name: User selection
    title: User selection
    authentication: none
  identifiers:
    slug: default-user-selection-flow
  model: authentik_flows.flow
  id: flow
- identifiers:
    name: default-user-selection-picker
  id: default-user-selection-picker
  model: authentik_stages_user_selection.userselectionstage
- identifiers:
    order: 10
    stage: !KeyOf default-user-selection-picker
    target: !KeyOf flow
  model: authentik_flows.flowstagebinding
