version: 1
metadata:
  name: Default - Authentication flow
entries:
- model: authentik_blueprints.metaapplyblueprint
  attrs:
    identifiers:
      name: Default - Password change flow
    required: false
- attrs:
    designation: authentication
    name: Welcome to authentik!
    title: Welcome to authentik!
    authentication: none
  identifiers:
    slug: default-authentication-flow
  model: authentik_flows.flow
  id: flow
- attrs:
    backends:
    - authentik.core.auth.InbuiltBackend
    - authentik.sources.kerberos.auth.KerberosBackend
    - authentik.sources.ldap.auth.LDAPBackend
    - authentik.core.auth.TokenBackend
    configure_flow: !Find [authentik_flows.flow, [slug, default-password-change]]
  identifiers:
    name: default-authentication-password
  id: default-authentication-password
  model: authentik_stages_password.passwordstage
- identifiers:
    name: default-authentication-mfa-validation
  id: default-authentication-mfa-validation
  model: authentik_stages_authenticator_validate.authenticatorvalidatestage
- attrs:
    user_fields:
    - email
    - username
  identifiers:
    name: default-authentication-identification
  id: default-authentication-identification
  model: authentik_stages_identification.identificationstage
- identifiers:
    name: default-authentication-login
  id: default-authentication-login
  model: authentik_stages_user_login.userloginstage
- identifiers:
    order: 10
    stage: !KeyOf default-authentication-identification
    target: !KeyOf flow
  model: authentik_flows.flowstagebinding
- identifiers:
    order: 20
    stage: !KeyOf default-authentication-password
    target: !KeyOf flow
  attrs:
    re_evaluate_policies: true
  id: default-authentication-flow-password-binding
  model: authentik_flows.flowstagebinding
- identifiers:
    order: 30
    stage: !KeyOf default-authentication-mfa-validation
    target: !KeyOf flow
  id: default-authentication-flow-authenticator-validation-binding
  model: authentik_flows.flowstagebinding
- identifiers:
    order: 100
    stage: !KeyOf default-authentication-login
    target: !KeyOf flow
  model: authentik_flows.flowstagebinding
# Replaced by conditional policies
- model: authentik_policies_expression.expressionpolicy
  state: absent
  identifiers:
    name: default-authentication-flow-password-stage
- model: authentik_policies_expression.expressionpolicy
  state: absent
  identifiers:
    name: default-authentication-flow-authenticator-validate-stage
- model: authentik_policies_conditional.conditionalpolicy
  id: default-authentication-flow-password-optional
  identifiers:
    name: default-authentication-flow-password-stage
  attrs:
    # Run the password stage unless the user was already authenticated earlier in the flow,
    # for example by an identification stage with a password field
    missing_behavior: "false"
    actions:
      version: 2
      actions:
        - type: condition
          condition:
            type: compare
            variable:
              key: plan.pending_user_authenticated
            operator: is_false
- model: authentik_policies_conditional.conditionalpolicy
  id: default-authentication-flow-authenticator-validate-optional
  identifiers:
    name: default-authentication-flow-authenticator-validate-stage
  attrs:
    # Skip the authenticator validation stage when the user authenticated passwordless with
    # WebAuthn
    missing_behavior: "false"
    actions:
      version: 2
      actions:
        - type: condition
          condition:
            type: group
            op: none
            children:
              - type: compare
                variable:
                  key: plan.auth_method
                operator: eq
                value:
                  type: literal
                  value: auth_webauthn_pwl
- model: authentik_policies.policybinding
  identifiers:
    order: 10
    target: !KeyOf default-authentication-flow-password-binding
    policy: !KeyOf default-authentication-flow-password-optional
  attrs:
    failure_result: true
- model: authentik_policies.policybinding
  identifiers:
    order: 10
    target: !KeyOf default-authentication-flow-authenticator-validation-binding
    policy: !KeyOf default-authentication-flow-authenticator-validate-optional
  attrs:
    failure_result: true
