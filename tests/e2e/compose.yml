---
services:
  postgresql:
    environment:
      POSTGRES_DB: authentik
      POSTGRES_USER: authentik
      POSTGRES_PASSWORD: ${PG_PASS:?database password required}
    ports:
      - "5432"
    healthcheck:
      interval: 30s
      retries: 5
      start_period: 20s
      test:
        - CMD-SHELL
        - pg_isready -d $${POSTGRES_DB} -U $${POSTGRES_USER}
      timeout: 5s
    image: docker.io/library/postgres:18-alpine
    restart: unless-stopped
  server:
    command: server
    depends_on:
      postgresql:
        condition: service_healthy
    environment:
      AUTHENTIK_POSTGRESQL__HOST: postgresql
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_POSTGRESQL__USER: authentik
      AUTHENTIK_POSTGRESQL__PASSWORD: ${PG_PASS}
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY:?secret key required}
      AUTHENTIK_LOG_LEVEL: debug
      AUTHENTIK_BOOTSTRAP_EMAIL: root@example.com
      AUTHENTIK_BOOTSTRAP_PASSWORD: akadmin
      AUTHENTIK_BOOTSTRAP_TOKEN: akadmin
    image: ${AUTHENTIK_IMAGE:-ghcr.io/goauthentik/dev-server}:${AUTHENTIK_TAG:?tag}
    ports:
      - "9000"
      - "9443"
      - "9300"
    restart: unless-stopped
    shm_size: 512mb
  worker:
    command: worker
    depends_on:
      postgresql:
        condition: service_healthy
    environment:
      AUTHENTIK_POSTGRESQL__HOST: postgresql
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_POSTGRESQL__USER: authentik
      AUTHENTIK_POSTGRESQL__PASSWORD: ${PG_PASS}
      AUTHENTIK_LOG_LEVEL: debug
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY:?secret key required}
      AUTHENTIK_BOOTSTRAP_EMAIL: root@example.com
      AUTHENTIK_BOOTSTRAP_PASSWORD: akadmin
      AUTHENTIK_BOOTSTRAP_TOKEN: akadmin
    image: ${AUTHENTIK_IMAGE:-ghcr.io/goauthentik/dev-server}:${AUTHENTIK_TAG:?tag}
    restart: unless-stopped
    shm_size: 512mb
    user: root
    ports:
      - "9300"
  selenium:
    profiles:
      - selenium
    image: ghcr.io/goauthentik/selenium:149.0-ak-0.50.2
    shm_size: 2g
    restart: always
    environment:
      VIDEO_READY_PORT: 9912
    ports:
      - "4444"
      - "7900"
      - "5900"
  mailpit:
    profiles:
      - mailpit
    image: docker.io/axllent/mailpit:v1.30.4
    ports:
      - "8025"
    healthcheck:
      test: ["CMD", "wget", "--spider", "http://localhost:8025"]
      interval: 5s
      start_period: 1s
  whoami:
    profiles:
      - whoami
    image: docker.io/traefik/whoami:v1.11.0
    ports:
      - "80"
  proxy:
    profiles:
      - proxy
    image: ${AUTHENTIK_IMAGE:-ghcr.io/goauthentik/dev-proxy}:${AUTHENTIK_TAG:?tag}
    environment:
      AUTHENTIK_HOST: http://server:9000
      AUTHENTIK_TOKEN: ${AUTHENTIK_PROXY_TOKEN:-}
    ports:
      - "9000"
      - "9443"
      - "9300"
