http:
  routers:
    authentik:
      rule: "PathPrefix(`/`)"
      entryPoints:
        - websecure
      service: authentik
      tls: {}
  services:
    authentik:
      loadBalancer:
        servers:
          - url: "http://host.docker.internal:8123"
