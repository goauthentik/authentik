:::info Redirect URI changes in authentik 2026.5
Before authentik 2026.5, all **Redirect URIs** are automatically of type `Authorization`. If you're running an older version, add the `Authorization` URL to your **Redirect URIs** and do not add the `Post Logout` URI.
:::
