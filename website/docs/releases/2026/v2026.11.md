---
title: Release 2026.11
slug: "/releases/2026.11"
draft: true
---

## Highlights

- **Password lockout**: :ak-enterprise Password lock a user's account after a configurable number of consecutive failed login attempts.

<!-- ## Breaking changes -->

## New features and improvements

### Password lockout :ak-enterprise

The [Password stage](../../add-secure-apps/flows-stages/stages/password/index.md#password-lockout) can now password lock a user's account after a set number of consecutive failed attempts, using the new **Failed attempts before lockout** option. This is disabled by default.

When a user account is password locked, authentication is denied even if the correct password is entered. This prevents further guessing attempts and blocks use of a compromised password until an administrator unlocks it. The attempt count is tracked per user, retained across sessions and authentication flows, and cleared after a successful login. Only password-based authentication is affected; a password locked user can still sign in through a flow that does not run a Password stage.

By default the login prompt returns the same error as it does for an incorrect password, preventing it from disclosing whether an account exists or is password locked. Optional last-attempt warning and lockout messages can be configured on the stage. Password lock and unlock actions are logged as `password_locked` and `password_unlocked` events.

Administrators can lock and unlock a user's password from that user's page in the Admin interface, and users can lock their own password to enforce passwordless sign-in. Changing the password clears the failure count but not the lock.

### Passwords stored as authenticator devices

A user's password hash now lives on a password device belonging to that user, rather than in a column on the user record, which brings passwords in line with every other authenticator. Existing passwords are migrated automatically on upgrade, and no configuration changes are required.

Password devices are deliberately excluded from multi-factor authentication: they do not appear in the device APIs and cannot be selected by an authenticator validation stage.

## Upgrading

This release does not introduce any new requirements. You can follow the upgrade instructions below; for more detailed information about upgrading authentik, refer to our [Upgrade documentation](../../install-config/upgrade.mdx).

:::warning Upgrade in order and upgrade all components
Upgrades MUST be performed sequentially by major version. If you are two or more major releases behind, you must first upgrade to each intermediate major release before upgrading to this one. Refer to our [Upgrade documentation](../../install-config/upgrade.mdx) for more information on upgrade sequence.

The version of the authentik instance and of any outposts must be the same. We recommend that you always upgrade any outposts at the same time you upgrade your authentik instance.
:::

### Docker Compose

To upgrade, download the new compose file and update the Docker stack with the new version, using these commands:

```shell
wget -O compose.yml https://goauthentik.io/version/xxxx.x/lifecycle/container/compose.yml
docker compose up -d
```

The `-O` flag retains the downloaded file's name, overwriting any existing local file with the same name.

### Kubernetes

Upgrade the Helm Chart to the new version, using the following commands:

```shell
helm repo update
helm upgrade authentik authentik/authentik -f values.yaml --version ^xxxx.x
```

## Minor changes/fixes

<!-- _Insert the output of `make gen-changelog` here_ -->

## API Changes

<!-- _Insert output of `make gen-diff` here_ -->
