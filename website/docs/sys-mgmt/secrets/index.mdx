---
title: Secrets
description: "Manage credentials shared by providers, sources, and stages."
authentik_version: "2026.11"
---

A secret holds a credential, such as an OAuth2 client secret, LDAP bind password, or SMTP password. Objects reference the secret, so they can share its value and use its permissions.

Manage secrets under **System** > **Secrets** in the Admin interface, or through the [API](./api-and-blueprints.mdx). Forms that reference a secret can also create one on the fly, and an OAuth2 or RADIUS provider created without one gets a secret with a generated value automatically.

## Types

A secret has one of three types, chosen when it is created:

- **Text**: a single-line value. This is the only type whose value authentik can generate and [rotate](./rotation.mdx).
- **Multi-line text**: a value spanning several lines, such as a PEM-encoded key or a JSON credential.
- **File**: an uploaded file, stored base64-encoded.

The type cannot be changed after the secret is created.

For Google service account credentials and Kubernetes kubeconfig, use multi-line text containing JSON or YAML, or upload the configuration as a file. Kerberos keytabs can be uploaded as files or entered as base64 or `TYPE:residual` text; credential caches accept an uploaded file or a `TYPE:residual` reference.

## Values

When creating a text secret, leave **Value** empty to generate a value. Enter an existing credential when it was issued by another system.

Use **View secret** to view and copy a text value, or **Download secret** to download a file. To replace a value, click **Edit** and supply **New value** or **New file**. Leaving it empty keeps the current value.

Viewing or downloading a value requires the **View secret's value** permission and is recorded as a [`secret_view`](../events/event-actions.mdx#secret_view) event.

## Permissions

Secrets use authentik's usual [permission system](../../users-sources/access-control/permissions.mdx), globally and per object. Beyond the standard add/change/delete/view set, two permissions apply to the value itself:

- **View secret's value**: read the value.
- **Rotate secret's value**: replace the value, either explicitly or with a generated one; see [Rotate a secret](./rotation.mdx).

Viewing a value or rotating a secret also requires **View Secret**. Replacing a value through the edit form requires **Change Secret** and **Rotate secret's value**. Rotation does not reveal the generated value unless the caller can view it.

Attaching a secret to another object requires **View secret's value**, because that object can send the credential to an external system. Editing an object while keeping its existing secret reference does not require this permission.

During upgrade, authentik copies existing role permissions onto each migrated secret. Roles retain their ability to view or replace credentials, including grants inherited through groups. Credentials that were write-only remain hidden. These are grants on individual secrets; existing roles do not gain access to unrelated secrets. The original credential columns are removed after their values are migrated. After migration, manage secret permissions separately from the objects that reference them. Configure [initial permissions](../../users-sources/access-control/permissions.mdx) for roles that create new secrets, including secrets generated when creating a provider.

## Objects that reference secrets

The following objects store their credentials as a reference to a secret:

- OAuth2 and Proxy providers (client and cookie secrets)
- RADIUS providers (shared secret)
- LDAP sources (bind password)
- Kerberos sources (sync password, keytabs, and credential caches)
- OAuth, Plex, and Telegram sources (consumer secrets and tokens)
- SCIM providers (token)
- Duo, captcha, SMS, and email stages (API credentials and SMTP passwords)
- Notification transports (webhook URL)
- Microsoft Entra and Fleet enterprise integrations (credentials)
- Google Workspace providers and Google Chrome Device Trust connectors and stages (service account credentials)
- Kubernetes service connections (kubeconfig)

## Secrets authentik does not generate

Credentials issued by another system, such as an OAuth source's consumer secret, an LDAP bind password, a Duo API key, or an SMTP password, are also stored as secrets. Create a secret containing the external value from the referencing form. Do not use the rotate action unless the other system accepts a value generated by authentik.
