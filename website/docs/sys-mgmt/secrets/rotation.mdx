---
title: Rotate a secret
description: "Replace a secret's value with a server-generated value."
authentik_version: "2026.11"
sidebar_position: 1
---

Rotating replaces a [secret](./index.mdx)'s value with a freshly generated one. Only text secrets can be rotated; multi-line and file secrets keep their value until you change it by hand.

The old value stops working immediately for every object referencing the secret, so update every client that uses it. Outposts whose providers use the secret get the updated configuration within a few seconds.

:::warning
Only rotate a secret whose value another system issued (an OAuth source's consumer secret, an SMTP password, and the like) if that system accepts the value authentik generates, which is rarely the case. See [Secrets authentik does not generate](./index.mdx#secrets-authentik-does-not-generate).
:::

Click the rotate icon on the secrets list under **System** > **Secrets** and confirm. This takes the **Rotate secret's value** permission.

Forms that select a secret expose the same action beside the selector. If you also have the **View secret's value** permission, authentik shows the new value after rotation so you can copy it to the client.

## Auditing

Rotation and explicit value replacement are recorded as a [`secret_rotate`](../events/event-actions.mdx#secret_rotate) event.
