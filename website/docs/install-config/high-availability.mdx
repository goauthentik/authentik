---
title: High availability
---

authentik supports various highly available deployment setups. Multiple instances of the server and worker containers can be run in parallel, allowing for failover and redundancy and the authentik database utilizes PostgreSQL which has extensive support for different highly available setups.
