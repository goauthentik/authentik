---
services:
    gen:
        image: docker.io/openapitools/openapi-generator-cli:v7.21.0
        restart: never
        network_mode: none
        volumes:
            - ./:/local
            - ../../schema.yml:/schema.yml:ro
