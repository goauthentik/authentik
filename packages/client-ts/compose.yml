---
services:
    gen:
        image: docker.io/openapitools/openapi-generator-cli:v7.24.0
        restart: never
        network_mode: none
        volumes:
            - ./:/local
            - ../../schema.yml:/schema.yml:ro
