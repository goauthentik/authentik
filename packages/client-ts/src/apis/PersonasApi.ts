/* tslint:disable */
/* eslint-disable */
/**
 * authentik
 * Making authentication simple.
 *
 * The version of the OpenAPI document: 2026.8.0-rc1
 * Contact: hello@goauthentik.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import {
    type PaginatedPersonaList,
    PaginatedPersonaListFromJSON,
} from "../models/PaginatedPersonaList";
import { type Persona, PersonaFromJSON } from "../models/Persona";
import {
    type PersonaCreateRequest,
    PersonaCreateRequestToJSON,
} from "../models/PersonaCreateRequest";
import * as runtime from "../runtime";

export interface PersonasPersonasCreateRequest {
    personaCreateRequest: PersonaCreateRequest;
}

export interface PersonasPersonasDestroyRequest {
    id: number;
}

export interface PersonasPersonasListRequest {
    ordering?: string;
    page?: number;
    pageSize?: number;
    search?: string;
}

export interface PersonasPersonasRetrieveRequest {
    id: number;
}

/**
 *
 */
export class PersonasApi extends runtime.BaseAPI {
    /**
     * Creates request options for personasPersonasCreate without sending the request
     */
    async personasPersonasCreateRequestOpts(
        requestParameters: PersonasPersonasCreateRequest,
    ): Promise<runtime.RequestOpts> {
        if (requestParameters["personaCreateRequest"] == null) {
            throw new runtime.RequiredError(
                "personaCreateRequest",
                'Required parameter "personaCreateRequest" was null or undefined when calling personasPersonasCreate().',
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters["Content-Type"] = "application/json";

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("authentik", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/personas/personas/`;

        return {
            path: urlPath,
            method: "POST",
            headers: headerParameters,
            query: queryParameters,
            body: PersonaCreateRequestToJSON(requestParameters["personaCreateRequest"]),
        };
    }

    /**
     * Admin-provisioned delegate identities. An admin creates a Persona for a given parent user, then grants it access the same way as any other User -- ordinary PolicyBindings pointed at whatever it needs.
     */
    async personasPersonasCreateRaw(
        requestParameters: PersonasPersonasCreateRequest,
        initOverrides?: RequestInit | runtime.InitOverrideFunction,
    ): Promise<runtime.ApiResponse<Persona>> {
        const requestOptions = await this.personasPersonasCreateRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PersonaFromJSON(jsonValue));
    }

    /**
     * Admin-provisioned delegate identities. An admin creates a Persona for a given parent user, then grants it access the same way as any other User -- ordinary PolicyBindings pointed at whatever it needs.
     */
    async personasPersonasCreate(
        requestParameters: PersonasPersonasCreateRequest,
        initOverrides?: RequestInit | runtime.InitOverrideFunction,
    ): Promise<Persona> {
        const response = await this.personasPersonasCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for personasPersonasDestroy without sending the request
     */
    async personasPersonasDestroyRequestOpts(
        requestParameters: PersonasPersonasDestroyRequest,
    ): Promise<runtime.RequestOpts> {
        if (requestParameters["id"] == null) {
            throw new runtime.RequiredError(
                "id",
                'Required parameter "id" was null or undefined when calling personasPersonasDestroy().',
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("authentik", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/personas/personas/{id}/`;
        urlPath = urlPath.replace("{id}", encodeURIComponent(String(requestParameters["id"])));

        return {
            path: urlPath,
            method: "DELETE",
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Admin-provisioned delegate identities. An admin creates a Persona for a given parent user, then grants it access the same way as any other User -- ordinary PolicyBindings pointed at whatever it needs.
     */
    async personasPersonasDestroyRaw(
        requestParameters: PersonasPersonasDestroyRequest,
        initOverrides?: RequestInit | runtime.InitOverrideFunction,
    ): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.personasPersonasDestroyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Admin-provisioned delegate identities. An admin creates a Persona for a given parent user, then grants it access the same way as any other User -- ordinary PolicyBindings pointed at whatever it needs.
     */
    async personasPersonasDestroy(
        requestParameters: PersonasPersonasDestroyRequest,
        initOverrides?: RequestInit | runtime.InitOverrideFunction,
    ): Promise<void> {
        await this.personasPersonasDestroyRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for personasPersonasList without sending the request
     */
    async personasPersonasListRequestOpts(
        requestParameters: PersonasPersonasListRequest,
    ): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters["ordering"] != null) {
            queryParameters["ordering"] = requestParameters["ordering"];
        }

        if (requestParameters["page"] != null) {
            queryParameters["page"] = requestParameters["page"];
        }

        if (requestParameters["pageSize"] != null) {
            queryParameters["page_size"] = requestParameters["pageSize"];
        }

        if (requestParameters["search"] != null) {
            queryParameters["search"] = requestParameters["search"];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("authentik", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/personas/personas/`;

        return {
            path: urlPath,
            method: "GET",
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Admin-provisioned delegate identities. An admin creates a Persona for a given parent user, then grants it access the same way as any other User -- ordinary PolicyBindings pointed at whatever it needs.
     */
    async personasPersonasListRaw(
        requestParameters: PersonasPersonasListRequest,
        initOverrides?: RequestInit | runtime.InitOverrideFunction,
    ): Promise<runtime.ApiResponse<PaginatedPersonaList>> {
        const requestOptions = await this.personasPersonasListRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) =>
            PaginatedPersonaListFromJSON(jsonValue),
        );
    }

    /**
     * Admin-provisioned delegate identities. An admin creates a Persona for a given parent user, then grants it access the same way as any other User -- ordinary PolicyBindings pointed at whatever it needs.
     */
    async personasPersonasList(
        requestParameters: PersonasPersonasListRequest = {},
        initOverrides?: RequestInit | runtime.InitOverrideFunction,
    ): Promise<PaginatedPersonaList> {
        const response = await this.personasPersonasListRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for personasPersonasRetrieve without sending the request
     */
    async personasPersonasRetrieveRequestOpts(
        requestParameters: PersonasPersonasRetrieveRequest,
    ): Promise<runtime.RequestOpts> {
        if (requestParameters["id"] == null) {
            throw new runtime.RequiredError(
                "id",
                'Required parameter "id" was null or undefined when calling personasPersonasRetrieve().',
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("authentik", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/personas/personas/{id}/`;
        urlPath = urlPath.replace("{id}", encodeURIComponent(String(requestParameters["id"])));

        return {
            path: urlPath,
            method: "GET",
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Admin-provisioned delegate identities. An admin creates a Persona for a given parent user, then grants it access the same way as any other User -- ordinary PolicyBindings pointed at whatever it needs.
     */
    async personasPersonasRetrieveRaw(
        requestParameters: PersonasPersonasRetrieveRequest,
        initOverrides?: RequestInit | runtime.InitOverrideFunction,
    ): Promise<runtime.ApiResponse<Persona>> {
        const requestOptions = await this.personasPersonasRetrieveRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PersonaFromJSON(jsonValue));
    }

    /**
     * Admin-provisioned delegate identities. An admin creates a Persona for a given parent user, then grants it access the same way as any other User -- ordinary PolicyBindings pointed at whatever it needs.
     */
    async personasPersonasRetrieve(
        requestParameters: PersonasPersonasRetrieveRequest,
        initOverrides?: RequestInit | runtime.InitOverrideFunction,
    ): Promise<Persona> {
        const response = await this.personasPersonasRetrieveRaw(requestParameters, initOverrides);
        return await response.value();
    }
}
