---
services:
  diff:
    image: docker.io/openapitools/openapi-diff:2.1.6
    restart: never
    network_mode: none
    volumes:
      - ../../:/local

  gen:
    image: docker.io/openapitools/openapi-generator-cli:v7.18.0
    restart: never
    network_mode: none
    volumes:
      - ../../:/local
