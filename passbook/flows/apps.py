"""passbook Flows AppConfig"""

from django.apps import AppConfig


class PassbookFlowsConfig(AppConfig):
    """passbook Flows Config"""

    name = "passbook.flows"
    label = "passbook_flows"
    # mountpoint = "flows/"
    verbose_name = "passbook Flows"
